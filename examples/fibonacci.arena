fn main() =
    if fib(40) == 102334155 then print("CORRECT\n") else print("WRONG\n")

fn foo(n: i32) -> i32 = let x = n; let y = n; if y == 1 then x else n

fn fib(n: i32) -> i32 =
    if n == 0 || n == 1 then n
    else fib(n-1) + fib(n-2)
