// Result:
// CORRECT

fn main() =
    // TODO: When printInt fix tests
    if fib(40) == 102334155 then print("CORRECT\n") else print("WRONG\n")

fn fib(n: i32) -> i32 =
    if n == 0 || n == 1 then n
    else fib(n-1) + fib(n-2)
